{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Home","text":""},{"location":"#team-techno-economic-assessment-and-manipulation-framework","title":"TEAM: Techno-economic assessment and manipulation framework","text":"<p>TEAM stands for Techno-Economic Assessment and Manipulation framework. It is pronounced the same way as the English word team /ti\u02d0m/.</p> <p>TEAM is a framework for performing techno-economic assessments in the context of energy and climate-mitigation studies.</p> <ul> <li> <p> User Guide</p> <p>Understand the basic concepts of TEAM.</p> <p> Read the guide</p> </li> <li> <p> Examples</p> <p>Look at examples of techno-economic assessments made with TEAM.</p> <p> Look at the examples</p> </li> <li> <p> API Reference</p> <p>Inspect the functions and classes of the TEAM framework written in Python.</p> <p> Read the code docs</p> </li> </ul>"},{"location":"#credits-and-thanks","title":"Credits and thanks","text":"<ul> <li>The software code has been written by P.C. Verpoort.</li> <li>This work has been completed at the Potsdam Institute for Climate Impact Research (PIK), a German research institute conducting integrated research for global sustainability.</li> <li>This work has been completed as part of the Ariadne project with funding from the German Federal Ministry of Research, Technology and Space (grant nos. 03SFK5A, 03SFK5A0-2).</li> </ul>"},{"location":"#how-to-cite","title":"How to cite","text":"<ul> <li>To cite a release (recommended), please refer to a specific version archived on Zenodo (which will soon be made available).</li> <li>To cite a specific commit, please refer to the citation information in <code>CITATION.cff</code> and include the commit hash.</li> </ul>"},{"location":"#licence","title":"Licence","text":"<p>The software in this repository is licensed under an  MIT Licence.</p>"},{"location":"api/","title":"API Reference","text":"<p>TEAM: Techno-economic assessment and manipulation framework.</p> <p>TEAM is a framework for performing techno-economic assessments in the context of energy and climate-mitigation studies.</p>"},{"location":"api/#team.team_accessor","title":"<code>team_accessor</code>","text":"Source code in <code>team/_accessor.py</code> <pre><code>@pd.api.extensions.register_dataframe_accessor(\"team\")\nclass TEAMAccessor:\n    def __init__(self, df: pd.DataFrame):\n        # Check that column axis has only one level.\n        if df.columns.nlevels &gt; 1:\n            raise ValueError(\n                \"Can only use .team accessor with team-like dataframes that \"\n                \"contain only one column layer.\"\n            )\n\n        # Warn if 'unfielded' column exists.\n        if \"unfielded\" in df.columns:\n            warnings.warn(\n                \"Having a column named 'unfielded' in the dataframe \"\n                \"may result in unexpected behaviour.\"\n            )\n\n        # Store dataframe as member field.\n        self._df = df\n\n    def fields(\n        self,\n        var_cols: str | list[str] | tuple[str] = \"variable\",\n        value_col: str = \"value\",\n        unit_col: str = \"unit\",\n    ) -&gt; list[str]:\n        \"\"\"Return list of column names of fields.\n\n        Parameters\n        ----------\n        var_cols : str or list[str] or tuple[str], optional\n            Name of column containing the variables as strings. Default is\n            `variable`. Multiple columns can be defined for advanced pivotting\n            (with multi-level column index), e.g. when pivotting both\n            `variable` and `period`.\n        value_col : str, optional\n            Name of the column containing the value. Default is `value`.\n        unit_col : str, optional\n            Name of the column containing the unit. Default is `unit`.\n\n        Returns\n        -------\n            list of str\n                A list of strings corresponding to the column names.\n\n        \"\"\"\n        if isinstance(var_cols, str):\n            var_cols = [var_cols]\n        return [\n            c for c in self._df if c not in ([value_col, unit_col] + var_cols)\n        ]\n\n    def sort_columns(\n        self,\n        fields_order: list[str] | None = None,\n        var_cols: str | list[str] | tuple[str] = \"variable\",\n        value_col: str = \"value\",\n        unit_col: str = \"unit\",\n    ) -&gt; pd.DataFrame:\n        \"\"\"Sort columns of a dataframe based on the TEAM logic.\n\n        This will sort fields in the beginning, followed by variable, unit,\n        and value columns. By passing `fields_order`, the order for the fields\n        can be manually set.\n\n        Parameters\n        ----------\n        fields_order : list[str], optional\n            Option for manually defining the order of field columns that come\n            before the variable, unit, and value columns.\n        var_cols : str or list[str] | tuple[str], optional\n            Option to override name of the variable columns. Default is\n            `variable`.\n        value_col : str, optional\n            Option to override name of the value column. Default is `value`.\n        unit_col : str, optional\n            Option to override name of the unit column. Default is `unit`.\n\n        \"\"\"\n        return _sort_cols(\n            df=self._df,\n            fields_order=fields_order or [],\n            var_cols=(\n                [var_cols] if isinstance(var_cols, str) else list(var_cols)\n            ),\n            value_col=value_col,\n            unit_col=unit_col,\n        )\n\n    def sort_rows(\n        self,\n        by: str | list[str] | None = None,\n        var_cols: str | list[str] | tuple[str] = \"variable\",\n        value_col: str = \"value\",\n        unit_col: str = \"unit\",\n    ):\n        \"\"\"Sort rows of a dataframe based on the TEAM logic.\n\n        This will sort rows either by the columns provided by argument `by` or,\n        if no `by` is provided, by all fields and the variable column.\n\n        Parameters\n        ----------\n        by : str | list[str], optional\n            Option for manually defining the columns by which to sort.\n        var_cols : str or list[str] | tuple[str], optional\n            Option to override name of the variable columns. Default is\n            `variable`.\n        value_col : str, optional\n            Option to override name of the value column. Default is `value`.\n        unit_col : str, optional\n            Option to override name of the unit column. Default is `unit`.\n\n        \"\"\"\n        if by is None:\n            var_cols = (\n                [var_cols] if isinstance(var_cols, str) else list(var_cols)\n            )\n            base_cols = var_cols + [value_col, unit_col]\n\n            by = [c for c in self._df if c not in base_cols]\n            if all(v in self._df for v in var_cols):\n                by += var_cols\n        elif not by:\n            raise Exception(\"Argument `by` may not be an empty list or str.\")\n\n        return self._df.sort_values(by=by).reset_index(drop=True)\n\n    def explode(\n        self,\n        fields: Optional[str | list[str]] = None,\n        var_cols: str | list[str] | tuple[str] = \"variable\",\n        value_col: str = \"value\",\n        unit_col: str = \"unit\",\n        field_values: pd.DataFrame | None = None,\n    ) -&gt; pd.DataFrame:\n        \"\"\"Explode rows with NaN entries.\n\n        Parameters\n        ----------\n        fields : str | list[str] | None\n            The list of fields to explode.\n        var_cols : str or list[str] or tuple[str], optional\n            Name of column containing the variables as strings. Default is\n            `variable`. Multiple columns can be defined for advanced pivoting\n            (with multi-level column index), e.g. when pivoting both\n            `variable` and `period`.\n        value_col : str, optional\n            Name of the column containing the value. Default is `value`.\n        unit_col : str, optional\n            Name of the column containing the unit. Default is `unit`.\n        field_values : pd.DataFrame, optional\n            Field values to use. By default, the field values will be taken\n            from existing data.\n\n        Returns\n        -------\n            pd.DataFrame\n                The dataframe with nan entries in the respective fields\n                exploded.\n\n        \"\"\"\n        df = self._df\n\n        if fields is None:\n            fields = self.fields(\n                var_cols=var_cols,\n                value_col=value_col,\n                unit_col=unit_col,\n            )\n        else:\n            if isinstance(fields, str):\n                fields = [fields]\n            for field_id in fields:\n                if field_id not in df.columns:\n                    df = df.assign(**{field_id: np.nan})\n\n        if field_values is not None:\n            if not set(fields).issubset(field_values.columns):\n                raise Exception(\n                    f\"field_values must contain all field columns. \"\n                    f\"Looking for `{','.join(fields)}` but found \"\n                    f\"`{','.join(field_values.columns.tolist())}`\"\n                )\n            # Only keep the relevant columns\n            field_values = field_values[fields]\n            if field_values.duplicated().any():\n                raise Exception(\n                    \"field_values may not contain duplicates:\\n\"\n                    + str(field_values)\n                )\n            if field_values.isnull().any().any():\n                raise Exception(\n                    \"field_values may not contain NaNs:\\n\" + str(field_values)\n                )\n\n        if not fields:\n            return df\n\n        # Derive allowed combinations once.\n        if field_values is not None:\n            allowed = (\n                field_values[fields].drop_duplicates().reset_index(drop=True)\n            )\n        else:\n            allowed = _get_allowed_combinations(df, fields)\n\n        # Explode each column to all its unique non-NaN values\n        for field_id in fields:\n            all_values = df[field_id].dropna().unique().tolist()\n            df = df.assign(\n                **{\n                    field_id: pd.Series(\n                        len(df) * [all_values], index=df.index\n                    ).where(df[field_id].isnull(), other=df[field_id])\n                }\n            )\n            df = df.explode(field_id)\n\n        return df.merge(allowed, on=fields, how=\"inner\").reset_index(drop=True)\n\n    def groupby_fields(\n        self,\n        fields: Optional[str | list[str]] = None,\n        var_cols: str | list[str] | tuple[str] = \"variable\",\n        value_col: str = \"value\",\n        unit_col: str = \"unit\",\n        **kwargs,\n    ) -&gt; DataFrameGroupBy:\n        \"\"\"Group by fields columns.\n\n        Group by field columns (region, period, other...). Fields with rows\n        that contain nan entries will be 'exploded' first. Fields can be\n        passed explicitly or inferred from the column names of the dataframe\n        that are not the variable, unit, or value columns.\n\n        Parameters\n        ----------\n        fields : str | list[str], optional\n            Single field or list of fields to group by.\n        var_cols : str or list[str] or tuple[str], optional\n            Name of column containing the variables as strings. Default is\n            `variable`. Multiple columns can be defined for advanced pivotting\n            (with multi-level column index), e.g. when pivotting both\n            `variable` and `period`.\n        value_col : str, optional\n            Name of the column containing the value. Default is `value`.\n        unit_col : str, optional\n            Name of the column containing the unit. Default is `unit`.\n        kwargs\n            Passed on to pd.DataFrame.groupby.\n\n        Returns\n        -------\n            pd.DataFrameGroupBy\n                The grouped dataframe rows.\n\n        \"\"\"\n        if \"by\" in kwargs:\n            raise Exception(\n                \"Cannot use 'by' argument. Please use `fields` arugment.\"\n            )\n        if fields is None:\n            fields = self.fields(\n                var_cols=var_cols,\n                value_col=value_col,\n                unit_col=unit_col,\n            )\n        return self.explode(fields=fields).groupby(by=fields, **kwargs)\n\n    def pivot_wide(\n        self,\n        var_cols: str | list[str] | tuple[str] = \"variable\",\n        value_col: str = \"value\",\n        unit_col: str = \"unit\",\n    ):\n        \"\"\"Pivot dataframe wide, such that column names are variables.\n\n        Parameters\n        ----------\n        var_cols : str or list[str] or tuple[str], optional\n            Name of column containing the variables as strings. Default is\n            `variable`. Multiple columns can be defined for advanced pivotting\n            (with multi-level column index), e.g. when pivotting both\n            `variable` and `period`.\n        value_col : str, optional\n            Name of the column containing the value. Default is `value`.\n        unit_col : str, optional\n            Name of the column containing the unit. Default is `unit`.\n\n        Returns\n        -------\n            pd.DataFrame\n                The original dataframe in pivot mode.\n\n        \"\"\"\n        fields = self.fields(\n            var_cols=var_cols,\n            value_col=value_col,\n            unit_col=unit_col,\n        )\n        ret = self.explode(\n            fields=fields,\n        )\n\n        # Create dummy field if non exists.\n        if not fields:\n            ret = ret.assign(unfielded=0)\n            fields = [\"unfielded\"]\n\n        # Set variable columns.\n        if isinstance(var_cols, str):\n            var_cols = [var_cols]\n\n        # Pivot dataframe.\n        ret = ret.pivot(\n            index=fields,\n            columns=var_cols + [unit_col],\n            values=value_col,\n        )\n\n        # Raise exception if duplicate cases exist.\n        if ret.index.has_duplicates:\n            raise ValueError(\n                \"Performed pivot_wide on dataframe with \"\n                \"duplicate cases. Each variable should only be \"\n                \"defined once for each combination of field \"\n                \"values.\"\n            )\n\n        return ret.pint.quantify()\n\n    def perform(\n        self,\n        func: Callable,\n        pre_mapper: dict | Callable | None = None,\n        post_mapper: dict | Callable | None = None,\n        using: pd.DataFrame | list[pd.DataFrame] | None = None,\n        only_new: bool = False,\n        fill_missing: bool = False,\n        dropna: bool = True,\n        var_cols: str | list[str] | tuple[str] = \"variable\",\n        value_col: str = \"value\",\n        unit_col: str = \"unit\",\n        args: tuple = (),\n        **kwargs,\n    ):\n        \"\"\"Perform a manipulation by applying a function to the dataframe.\n\n        Parameters\n        ----------\n        func : Callable\n            Function to apply to dataframe. The function should accept a\n            dataframe and return a dataframe. Additional arguments can be\n            supplied through the key-word arguments.\n        pre_mapper : dict-like or function, optional\n            Dict-like or function transformations to apply to column names\n            before passing dataframe as input to `func`.\n        post_mapper : dict-like or function, optional\n            Dict-like or function transformations to apply to column names\n            before processing output from `func`.\n        only_new : bool, optional\n            Whether to only keep new variables. Default is False.\n        fill_missing : bool, optional\n            Whether to impute missing variables with NaN in variable-unit\n            groups.\n        using : pd.DataFrame or list[pd.DataFrame], optional\n            Data/assumptions to use but not append to the output dataframe.\n        dropna: bool, optional\n            Whether to drop NaN entries resulting from the function call.\n            Default is True.\n        var_cols : str or list[str] or tuple[str], optional\n            Name of column containing the variables as strings. Default is\n            `variable`. Multiple columns can be defined for advanced pivotting\n            (with multi-level column index), e.g. when pivotting both\n            `variable` and `period`.\n        value_col : str, optional\n            Name of the column containing the value. Default is `value`.\n        unit_col : str, optional\n            Name of the column containing the unit. Default is `unit`.\n        args : tuple\n            Positional arguments to pass to `func` in addition to the data\n            contained in the dataframe.\n        **kwargs\n            Additional keyword arguments to pass to `func` in addition to the\n            data contained in the dataframe.\n\n        Returns\n        -------\n            pd.DataFrame\n                The dataframe that underwent the manipulation.\n\n        \"\"\"\n        return self.perform_multi(\n            [\n                dict(\n                    func=func,\n                    pre_mapper=pre_mapper,\n                    post_mapper=post_mapper,\n                    args=args,\n                    **kwargs,\n                )\n            ],\n            using=using,\n            only_new=only_new,\n            fill_missing=fill_missing,\n            dropna=dropna,\n            var_cols=var_cols,\n            value_col=value_col,\n            unit_col=unit_col,\n        )\n\n    def perform_multi(\n        self,\n        funcs: list[dict],\n        using: pd.DataFrame | list[pd.DataFrame] | None = None,\n        only_new: bool = False,\n        dropna: bool = True,\n        fill_missing: bool = False,\n        var_cols: str | list[str] | tuple[str] = \"variable\",\n        value_col: str = \"value\",\n        unit_col: str = \"unit\",\n    ):\n        \"\"\"Perform multiple manipulations to a dataframe at once.\n\n        Parameters\n        ----------\n        funcs : list[dict]\n            A list of dictionaries, where each dictionary contains arguments\n            `func` and (optionally) `pre_mapper`, `post_mapper`, `only_new`,\n            `dropna`, `args`, and `kwargs` (same as `.team.perform()`).\n        only_new : bool, optional\n            Whether to only keep new variables. Default is False.\n        fill_missing : bool, optional\n            Whether to impute missing variables with NaN in variable-unit\n            groups.\n        using : pd.DataFrame or list[pd.DataFrame], optional\n            Data/assumptions to use but not append to the output dataframe.\n        dropna: bool, optional\n            Whether to drop NaN entries resulting from the function call.\n            Default is True.\n        var_cols : str or list[str] or tuple[str], optional\n            Name of column containing the variables as strings. Default is\n            `variable`. Multiple columns can be defined for advanced pivotting\n            (with multi-level column index), e.g. when pivotting both\n            `variable` and `period`.\n        value_col : str, optional\n            Name of the column containing the value. Default is `value`.\n        unit_col : str, optional\n            Name of the column containing the unit. Default is `unit`.\n\n        Returns\n        -------\n            pd.DataFrame\n                The dataframe that underwent the manipulation(s).\n\n        \"\"\"\n        # Convert var_cols to list if tuple.\n        if isinstance(var_cols, tuple):\n            var_cols = list(var_cols)\n        elif not isinstance(var_cols, list):\n            var_cols = [var_cols]\n\n        # The old rows are the original dataframe.\n        df_old = self._df\n\n        # Depending on the arguments, we may combine this with `using` to\n        # a new dataframe called df_old_full.\n        if using is not None:\n            if isinstance(using, list | tuple) and all(\n                isinstance(u, pd.DataFrame) for u in using\n            ):\n                using = pd.concat(using)\n            elif not isinstance(using, pd.DataFrame):\n                raise Exception(\n                    \"Argument `using` must be dataframe or list of dataframes.\"\n                )\n\n            df_old_full = pd.concat([df_old, using])\n        else:\n            df_old_full = df_old\n\n        # Explode.\n        fields = df_old_full.team.fields(\n            var_cols=var_cols,\n            unit_col=unit_col,\n            value_col=value_col,\n        )\n        df_old_full = TEAMAccessor(df_old_full).explode(fields=fields)\n\n        # Create dummy field if none exists.\n        if not fields:\n            fields = [\"unfielded\"]\n            df_old_full = df_old_full.assign(unfielded=0)\n\n        # Group by all fields, by variable columns, and by unit. Verify\n        # integrity to ensure that there are no duplicates.\n        df_old_full = df_old_full.set_index(\n            fields + var_cols + [unit_col], verify_integrity=True\n        )\n\n        # Pivot dataframe and set pint units.\n        df_pivot = df_old_full[value_col].unstack(var_cols + [unit_col])\n\n        # Get a presence matrix.\n        presence = pd.Series(True, index=df_old_full.index).unstack(\n            var_cols + [unit_col], fill_value=False\n        )\n\n        # Determine presence groups.\n        pivot_groups = presence.groupby([c for c in presence.columns]).groups\n\n        # Loop over pivot groups.\n        return_list = []\n        for pivot_group_cols, pivot_group_row in pivot_groups.items():\n            # Get column names from boolean indexer.\n            pivot_group_cols = df_pivot.columns[\n                pd.Series(pivot_group_cols, index=df_pivot.columns)\n            ]\n\n            # Make sure that there are no columns with more than one unit\n            # defined per variable.\n            if not pivot_group_cols.droplevel(level=unit_col).is_unique:\n                raise Exception(\n                    \"Variable defined with multiple units: \"\n                    + df_pivot.loc[pivot_group_row]\n                )\n\n            # Get the pivot group by selecting relevant rows and columns and\n            # using pint to set the dtype of the column from the unit level.\n            df_pivot_group = df_pivot.loc[\n                pivot_group_row, pivot_group_cols\n            ].pint.quantify()\n\n            # Keep a copy of the index to ensure that it is not changed.\n            original_index = df_pivot_group.index\n\n            # Insert missing columns. This will be necessary in case the\n            # performed actions operate on column not present in this group.\n            if fill_missing:\n                cols = (\n                    df_pivot.columns.to_frame()\n                    .droplevel(unit_col)[unit_col]\n                    .to_dict()\n                )\n                missing_cols = {\n                    col_id: col_unit\n                    for col_id, col_unit in cols.items()\n                    if col_id not in df_pivot_group.columns\n                }\n                for col_id, col_unit in missing_cols.items():\n                    df_pivot_group[col_id] = pd.Series(\n                        np.nan, dtype=f\"pint[{col_unit}][float64]\"\n                    )\n\n            # Apply functions.\n            for func_specs in funcs:\n                # Fetch from dict.\n                func = func_specs.get(\"func\")\n                pre_mapper = func_specs.get(\"pre_mapper\")\n                post_mapper = func_specs.get(\"post_mapper\")\n                args = func_specs.get(\"args\") or ()\n                kwargs = {\n                    k: v\n                    for k, v in func_specs.items()\n                    if k not in [\"func\", \"pre_mapper\", \"post_mapper\", \"args\"]\n                }\n\n                # Raise Exception if func not provided.\n                if func is None:\n                    raise Exception(\"No function provided to call.\")\n                elif not isinstance(func, Callable):\n                    raise Exception(\"Function provided must be callable.\")\n\n                # Prepare data for function call.\n                df_pivot_group_mapped = (\n                    (df_pivot_group.rename(columns=pre_mapper))\n                    if pre_mapper is not None\n                    else df_pivot_group\n                )\n\n                # Call function.\n                df_pivot_group_ret = func(\n                    df_pivot_group_mapped, *args, **kwargs\n                )\n\n                # Check results. Skip if result is empty.\n                if df_pivot_group_ret is None:\n                    continue\n                elif isinstance(df_pivot_group_ret, dict):\n                    df_pivot_group_ret = pd.DataFrame(df_pivot_group_ret)\n                elif not isinstance(df_pivot_group_ret, pd.DataFrame):\n                    raise Exception(\"Return type must be a dict or dataframe.\")\n                if (\n                    df_pivot_group_ret.empty\n                    or df_pivot_group_ret.isnull().all().all()\n                ):\n                    continue\n                if not df_pivot_group_ret.index.equals(original_index):\n                    raise Exception(\"Manipulation may not change the index.\")\n\n                # Postprocess results and add to list.\n                if post_mapper is not None:\n                    df_pivot_group_ret = df_pivot_group_ret.rename(\n                        columns=post_mapper\n                    )\n\n                # Drop dummy columns.\n                if fill_missing and missing_cols:\n                    df_pivot_group_ret.drop(\n                        columns=list(missing_cols),\n                        errors=\"ignore\",\n                        inplace=True,\n                    )\n\n                # Combine results from functions, melt, and append to list.\n                return_list.append(\n                    df_pivot_group_ret\n                    # Ensure that the axis label still exists before melt.\n                    .rename_axis(var_cols, axis=1)\n                    # Split units off into separate column.\n                    .pint.dequantify()\n                    # Melt.\n                    .melt(ignore_index=False)\n                    # Append var and unit as index.\n                    .set_index(var_cols + [unit_col], append=True)\n                )\n\n        # Check whether return list is empty.\n        if not return_list:\n            if only_new:\n                return pd.DataFrame(columns=df_old.columns)\n            else:\n                return df_old\n\n        # Combine groups into single dataframe.\n        df_new = pd.concat(return_list)\n\n        # Convert pint objects to unit string and float value if any.\n        if df_new[value_col].dtype == \"object\":\n            df_new = df_new.reset_index(level=unit_col)\n            locs = df_new[value_col].apply(lambda x: isinstance(x, Quantity))\n            df_new.loc[locs, [unit_col, value_col]] = df_new.loc[\n                locs, value_col\n            ].apply(lambda x: pd.Series({unit_col: x.u, value_col: x.m}))\n            df_new = df_new.set_index(unit_col, append=True)\n\n        # Drop rows with nan entries in unit or value columns.\n        if dropna:\n            df_new.dropna(subset=value_col, inplace=True)\n\n        # Drop rows that are already in old.\n        common_idx = df_new.index.intersection(df_old_full.index)\n        same = (df_new.loc[common_idx] == df_old_full.loc[common_idx]).all(\n            axis=1\n        )\n        df_new = df_new.drop(common_idx[same])\n\n        # Raise exception if index has duplicates after the above.\n        if not df_new.index.is_unique:\n            duplicate_labels = df_new.loc[\n                df_new.index.duplicated(), fields + var_cols\n            ]\n            warnings.warn(\n                f\"Internal error: variables should only exist \"\n                f\"once per case: {duplicate_labels}\"\n            )\n\n        # Drop column called 'unfielded' if it exists.\n        if \"unfielded\" in df_new.index.names:\n            df_new = df_new.droplevel(\"unfielded\")\n\n        # Combine with old if requested.\n        if only_new:\n            ret = df_new\n        else:\n            df_old_extended_index = df_old.set_index(\n                [c for c in df_new.index.names if c in df_old.columns]\n            )\n            for level in df_new.index.names:\n                if level not in df_old_extended_index.index.names:\n                    df_old_extended_index = df_old_extended_index.set_index(\n                        pd.Index(\n                            [np.nan] * len(df_old_extended_index),\n                            name=level,\n                        ),\n                        append=True,\n                    )\n            ret = df_new.reset_index(level=unit_col).combine_first(\n                df_old_extended_index.reset_index(level=unit_col)\n            )\n\n        # Return.\n        return ret.reset_index()\n\n    def calculate(\n        self,\n        *expr_assignments: ExprAssignment,\n        only_new: bool = False,\n        fill_missing: bool = False,\n        var_cols: str | list[str] | tuple[str] = \"variable\",\n        value_col: str = \"value\",\n        unit_col: str = \"unit\",\n        **kw_assignments: KeywordAssignment,\n    ):\n        \"\"\"Calculate variables from expressions.\n\n        This accessor method allows calculating new variables without defining\n        a complete function to be used with `.perform()`. New variables can be\n        calculated through expression assignments or keyword assignments.\n        Expression assignments are of form \"`a` = `b` + `c`\" and are based on\n        the pandas eval functionality. Keyword assignment must be int,float,\n        string, or a function to be called to assign to the variable defined\n        as key.\n\n        Parameters\n        ----------\n        expr_assignments : ExprAssignment\n            Expression(s) for assigning variable. Expressions can be floats\n            or ints for direct assignment, strings for evaluation on the\n            dataframe, or callable to call on the dataframe.\n        only_new : bool\n            Whether only new values should be kept. Default is False.\n        fill_missing : bool, optional\n            Whether to impute missing variables with NaN in variable-unit\n            groups.\n        var_cols : str | list[str] | tuple[str]\n            The name(s) of the varaible column(s) to use. Default is\n            `variable`.\n        value_col : str\n            The name of the value column to use. Default is `value`.\n        unit_col : str\n            The name of the unit column to use. Default is `unit`.\n        kw_assignments : int | float | str | Callable\n            A value to assign to a column via keyword assignments.\n\n        Returns\n        -------\n        pd.DataFrame\n            The new dataframe with the newly calculated variables.\n\n        \"\"\"\n        return self.perform(\n            lambda df: _calculate(df, *expr_assignments, **kw_assignments),\n            only_new=only_new,\n            fill_missing=fill_missing,\n            var_cols=var_cols,\n            value_col=value_col,\n            unit_col=unit_col,\n        )\n\n    def varsplit(\n        self,\n        cmd: Optional[str] = None,\n        regex: Optional[str] = None,\n        var_col_new: str = \"variable\",\n        var_new: str | bool = True,\n        keep_unmatched: bool = False,\n    ) -&gt; pd.DataFrame:\n        \"\"\"Split variable column by pipe characters.\n\n        Split variable components separated by pipe characters into separate\n        columns. The pattern must either be provided as `cmd` or as `regex`.\n\n        Parameters\n        ----------\n        cmd : str\n            A command to interpret into a regex.\n        regex : str\n            A direct regex.\n        var_col_new : str\n            (Optional) The name of the column where the new\n            variable will be stored.\n        var_new : str\n            (Optional) The new variable name. True means, it is computed\n            automatically. A string will be used to override it. False means\n            that it is not added.\n        keep_unmatched : bool\n            Whether or not to keep unmatched rows.\n\n        Returns\n        -------\n            pd.DataFrame\n                The dataframe that contains the new split variables.\n\n        \"\"\"\n        # Check that precisely one of the two arguments (either `cmd`\n        # or `regex`) is provided.\n        if cmd is not None and regex is not None:\n            raise Exception(\n                \"Only one of the two arguments may be provided: cmd or regex.\"\n            )\n        if cmd is None and regex is None:\n            raise Exception(\n                \"Either a command or a regex string must be provided.\"\n            )\n\n        # Check that target is in columns of dataframe.\n        if var_col_new not in self._df.columns:\n            raise Exception(\n                f\"Could not find column of name '{var_col_new}' in dataframe.\"\n            )\n\n        # Determine regex from cmd if necessary.\n        if regex is None:\n            regex = (\n                \"^\"\n                + r\"\\|\".join(\n                    [\n                        rf\"(?P&lt;{t[1:]}&gt;[^|]*)\"\n                        if t[0] == \"?\"\n                        else rf\"(?P&lt;{t[1:]}&gt;.*)\"\n                        if t[0] == \"*\"\n                        else escape(t)\n                        for t in cmd.split(\"|\")\n                    ]\n                )\n                + \"$\"\n            )\n\n        # Determine value of new variable column from arguments.\n        if var_new is False:\n            var_new = None\n        elif var_new is True:\n            if cmd is None:\n                var_new = None\n            else:\n                var_new = \"|\".join(\n                    [t for t in cmd.split(\"|\") if t[0] not in (\"?\", \"*\")]\n                )\n\n        # Create dataframe to be returned by applying regex to variable\n        # column and dropping unmatched rows.\n        matched = self._df[var_col_new].str.extract(regex)\n\n        # Drop unmatched rows if requested.\n        is_unmatched = matched.isna().any(axis=1)\n        matched = matched.drop(index=matched.loc[is_unmatched].index)\n\n        # Assign new variable column and drop if all are nan.\n        if var_col_new not in matched:\n            cond = matched.notnull().any(axis=1)\n            matched[var_col_new] = self._df[var_col_new]\n            matched.loc[cond, var_col_new] = var_new or np.nan\n            if var_new is None:\n                warnings.warn(\n                    \"New target column could not be set automatically.\"\n                )\n\n        # Drop variable column if all nan.\n        if matched[var_col_new].isnull().all():\n            matched.drop(columns=var_col_new, inplace=True)\n\n        # Combine with original dataframe.\n        if keep_unmatched:\n            df_combine = self._df.assign(\n                **{var_col_new: lambda df: df[var_col_new].where(is_unmatched)}\n            )\n        else:\n            df_combine = self._df.loc[matched.index].drop(columns=var_col_new)\n        ret = matched.combine_first(df_combine)\n\n        # Sort columns.\n        order = matched.columns.tolist() + self._df.columns.tolist()\n        ret.sort_index(\n            key=lambda cols: [order.index(c) for c in cols],\n            axis=1,\n            inplace=True,\n        )\n\n        # Return dataframe.\n        return ret\n\n    def varcombine(\n        self,\n        cmd: str | Callable,\n        keep_cols: bool = False,\n        var_col: str = \"variable\",\n    ) -&gt; pd.DataFrame:\n        \"\"\"Combine columns into new variable (or other column).\n\n        Parameters\n        ----------\n        cmd : str | Callable\n            How the new variable (or other column) should be assembled.\n        keep_cols : bool\n            Whether to keep the used columns.\n        var_col : str\n            (Optional) The name of the target column. By default, this\n            will be called `variable`.\n\n        Returns\n        -------\n            pd.DataFrame\n                The updated dataframe.\n\n        \"\"\"\n        ret = self._df.assign(\n            **{\n                var_col: self._df.apply(\n                    lambda row: (\n                        cmd.format(**row) if isinstance(cmd, str) else cmd(row)\n                    ),\n                    axis=1,\n                ),\n            }\n        )\n        return (\n            ret\n            if keep_cols\n            else ret.filter(\n                [\n                    col\n                    for col in ret\n                    if col == var_col\n                    or (isinstance(cmd, Callable) or f\"{{{col}}}\" not in cmd)\n                ]\n            )\n        )\n\n    def sum_over(\n        self,\n        over: str | list[str] | tuple[str],\n        value_col: str = \"value\",\n        unit_col: str = \"unit\",\n    ):\n        \"\"\"Sum up values over specified fields.\n\n        Parameters\n        ----------\n        over : str | list[str] | tuple[str]\n            The name of the field (column) to sum over.\n        value_col : str, optional\n            Name of the column containing the value. Default is `value`.\n        unit_col : str, optional\n            Name of the column containing the unit. Default is `unit`.\n\n        Returns\n        -------\n            pd.DataFrame\n                The aggregated dataframe.\n\n        \"\"\"\n        # Convert single string to list of strings.\n        if isinstance(over, str):\n            over = [over]\n\n        def _helper_sum_units(group):\n            q = (\n                group.groupby(unit_col)\n                .agg(\"sum\")\n                .reset_index()\n                .apply(\n                    lambda row: row[value_col] * Unit(row[unit_col]), axis=1\n                )\n                .sum()\n            )\n            return pd.Series(\n                {\n                    value_col: q.m,\n                    unit_col: f\"{q.u:~}\",\n                }\n            )\n\n        group_cols = [\n            c\n            for c in self._df\n            if c not in over and c not in [value_col, unit_col]\n        ]\n\n        if not group_cols:\n            # implement\n            pass\n\n        return (\n            self._df\n            # group by all columns except unit, value, and 'over'\n            .groupby(group_cols, dropna=False)[\n                # select only unit and value columns\n                [unit_col, value_col]\n            ]\n            # apply summation\n            .apply(\n                lambda group: (\n                    _helper_sum_units(group)\n                    if group[unit_col].nunique() &gt; 1\n                    else pd.Series(\n                        {\n                            value_col: group[value_col].sum(),\n                            unit_col: group[unit_col].iloc[0],\n                        }\n                    )\n                )\n            )\n            .reset_index()\n        )\n\n    def unit_to(\n        self,\n        other: str | Unit | dict[str, str | Unit],\n        *contexts: str | pint.Context,\n        var_cols: str | list[str] | tuple[str] = \"variable\",\n        value_col: str = \"value\",\n        unit_col: str = \"unit\",\n        **ctx_kwargs,\n    ):\n        \"\"\"Convert units in dataframe.\n\n        Parameters\n        ----------\n        other : str | team_units.Unit | dict[str, str | team_units.Unit]\n            The unit to convert to. This is either one unit for all rows\n            or a dict that maps variables to units.\n        contexts : str | pint.Context\n            A pint unit context to assume for unit conversion.\n        var_cols : str | list[str] | tuple[str]\n            The name(s) of the varaible column(s) to use. Default is\n            `variable`.\n        value_col : str\n            The name of the value column to use. Default is `value`.\n        unit_col : str\n            The name of the unit column to use. Default is `unit`.\n        ctx_kwargs : float\n            Constants or parameters to assume as part of the unit context.\n\n        Returns\n        -------\n            pd.DataFrame\n                The dataframe with updated units.\n\n        \"\"\"\n        return self.__unit_convert(\n            \"to\",\n            other=other,\n            contexts=contexts,\n            var_cols=var_cols,\n            value_col=value_col,\n            unit_col=unit_col,\n            ctx_kwargs=ctx_kwargs,\n        )\n\n    def unit_to_preferred(\n        self,\n        other: list[Unit] | dict[str, list[Unit]],\n        *contexts,\n        var_cols: str | list[str] | tuple[str] = \"variable\",\n        value_col: str = \"value\",\n        unit_col: str = \"unit\",\n        **ctx_kwargs,\n    ):\n        \"\"\"Convert units in dataframe.\n\n        Parameters\n        ----------\n        other : list[Unit] | dict[str, list[Unit]]\n            The preferred units to use for conversion. This is either one\n            list of units for all rows or a dict that maps variables to\n            lists of units.\n        contexts : str | pint.Context\n            A pint unit context to assume for unit conversion.\n        var_cols : str | list[str] | tuple[str]\n            The name(s) of the varaible column(s) to use. Default is\n            `variable`.\n        value_col : str\n            The name of the value column to use. Default is `value`.\n        unit_col : str\n            The name of the unit column to use. Default is `unit`.\n        ctx_kwargs\n            Constants or parameters to assume as part of the unit context.\n\n        Returns\n        -------\n            pd.DataFrame\n                The dataframe with updated units.\n\n        \"\"\"\n        return self.__unit_convert(\n            \"to_preferred\",\n            other=other,\n            contexts=contexts,\n            var_cols=var_cols,\n            value_col=value_col,\n            unit_col=unit_col,\n            ctx_kwargs=ctx_kwargs,\n        )\n\n    def __unit_convert(\n        self,\n        subfunc: str,\n        other: str\n        | Unit\n        | dict[str, str | Unit]\n        | list[Unit]\n        | dict[str, list[Unit]],\n        contexts: str | pint.Context,\n        var_cols: str | list[str] | tuple[str],\n        value_col: str,\n        unit_col: str,\n        ctx_kwargs: dict[str, float],\n    ):\n        # Check if to_unit is a single unit or a dict of units\n        if not isinstance(other, dict):\n            conv_factors = {}\n            conv_units = {}\n            if subfunc == \"to_preferred\":\n                for from_unit in self._df[unit_col].unique():\n                    from_unit_safe = from_unit or \"dimensionless\"\n                    other_q = getattr(Quantity(from_unit_safe), subfunc)(\n                        other, *contexts, **ctx_kwargs\n                    )\n                    conv_factors[from_unit] = other_q.m\n                    conv_units[from_unit] = f\"{other_q.u:~}\"\n                return self._df.assign(\n                    **{\n                        value_col: self._df[value_col]\n                        * self._df[unit_col].map(conv_factors),\n                        unit_col: self._df[unit_col].map(conv_units),\n                    }\n                )\n            else:\n                for from_unit in self._df[unit_col].unique():\n                    from_unit_safe = from_unit or \"dimensionless\"\n                    other_q = getattr(Quantity(from_unit_safe), subfunc)(\n                        other, *contexts, **ctx_kwargs\n                    )\n                    conv_factors[from_unit] = other_q.m\n                return self._df.assign(\n                    **{\n                        value_col: self._df[value_col]\n                        * self._df[unit_col].map(conv_factors),\n                        unit_col: str(other),\n                    }\n                )\n\n        else:\n            # Convert single string to list of strings.\n            if isinstance(var_cols, str):\n                var_cols = [var_cols]\n\n            # Check that var_cols is valid.\n            if not var_cols or any(c not in self._df for c in var_cols):\n                raise Exception(\n                    f\"Argument var_cols must contain valid column \"\n                    f\"name(s). Found: {var_cols}. Must be one of: \"\n                    f\"{', '.join(self._df.columns)}.\"\n                )\n\n            # Determine locations where conversion needs to take place.\n            if len(var_cols) == 1:\n                locs = self._df[var_cols[0]].isin(list(other))\n            else:\n                locs = (\n                    self._df[var_cols].apply(tuple, axis=1).isin(list(other))\n                )\n\n            # Define mappings for new units and conversion factors.\n            mapping = (\n                self._df.loc[locs, var_cols + [unit_col]]\n                .drop_duplicates()\n                .apply(\n                    lambda row: pd.Series(\n                        {\n                            \"index\": tuple(row),\n                            \"conv_unit\": other.get(\n                                tuple(row.iloc[0:-1])\n                                if len(row) &gt; 2\n                                else row.iloc[0],\n                                row.iloc[-1],\n                            ),\n                            \"conv_factor\": getattr(\n                                Quantity(row.iloc[-1]), subfunc\n                            )(\n                                other.get(\n                                    tuple(row.iloc[0:-1])\n                                    if len(row) &gt; 2\n                                    else row.iloc[0],\n                                    row.iloc[-1],\n                                ),\n                                *contexts,\n                                **ctx_kwargs,\n                            ).m,\n                        }\n                    ),\n                    axis=1,\n                )\n                .set_index(\"index\")\n            )\n            conv_factors = mapping[\"conv_factor\"].to_dict()\n            conv_unit = mapping[\"conv_unit\"].to_dict()\n\n            # Apply mapping.\n            mapping_key_col = self._df.loc[locs, var_cols + [unit_col]].apply(\n                tuple, axis=1\n            )\n            new_values = self._df.loc[locs, value_col] * mapping_key_col.map(\n                conv_factors\n            )\n            new_units = mapping_key_col.map(conv_unit)\n            return self._df.assign(\n                **{\n                    value_col: self._df[value_col].where(~locs, new_values),\n                    unit_col: self._df[unit_col].where(~locs, new_units),\n                }\n            )\n</code></pre>"},{"location":"api/#team.team_accessor.calculate","title":"<code>calculate(*expr_assignments, only_new=False, fill_missing=False, var_cols='variable', value_col='value', unit_col='unit', **kw_assignments)</code>","text":"<p>Calculate variables from expressions.</p> <p>This accessor method allows calculating new variables without defining a complete function to be used with <code>.perform()</code>. New variables can be calculated through expression assignments or keyword assignments. Expression assignments are of form \"<code>a</code> = <code>b</code> + <code>c</code>\" and are based on the pandas eval functionality. Keyword assignment must be int,float, string, or a function to be called to assign to the variable defined as key.</p> <p>Parameters:</p> Name Type Description Default <code>expr_assignments</code> <code>ExprAssignment</code> <p>Expression(s) for assigning variable. Expressions can be floats or ints for direct assignment, strings for evaluation on the dataframe, or callable to call on the dataframe.</p> <code>()</code> <code>only_new</code> <code>bool</code> <p>Whether only new values should be kept. Default is False.</p> <code>False</code> <code>fill_missing</code> <code>bool</code> <p>Whether to impute missing variables with NaN in variable-unit groups.</p> <code>False</code> <code>var_cols</code> <code>str | list[str] | tuple[str]</code> <p>The name(s) of the varaible column(s) to use. Default is <code>variable</code>.</p> <code>'variable'</code> <code>value_col</code> <code>str</code> <p>The name of the value column to use. Default is <code>value</code>.</p> <code>'value'</code> <code>unit_col</code> <code>str</code> <p>The name of the unit column to use. Default is <code>unit</code>.</p> <code>'unit'</code> <code>kw_assignments</code> <code>int | float | str | Callable</code> <p>A value to assign to a column via keyword assignments.</p> <code>{}</code> <p>Returns:</p> Type Description <code>DataFrame</code> <p>The new dataframe with the newly calculated variables.</p> Source code in <code>team/_accessor.py</code> <pre><code>def calculate(\n    self,\n    *expr_assignments: ExprAssignment,\n    only_new: bool = False,\n    fill_missing: bool = False,\n    var_cols: str | list[str] | tuple[str] = \"variable\",\n    value_col: str = \"value\",\n    unit_col: str = \"unit\",\n    **kw_assignments: KeywordAssignment,\n):\n    \"\"\"Calculate variables from expressions.\n\n    This accessor method allows calculating new variables without defining\n    a complete function to be used with `.perform()`. New variables can be\n    calculated through expression assignments or keyword assignments.\n    Expression assignments are of form \"`a` = `b` + `c`\" and are based on\n    the pandas eval functionality. Keyword assignment must be int,float,\n    string, or a function to be called to assign to the variable defined\n    as key.\n\n    Parameters\n    ----------\n    expr_assignments : ExprAssignment\n        Expression(s) for assigning variable. Expressions can be floats\n        or ints for direct assignment, strings for evaluation on the\n        dataframe, or callable to call on the dataframe.\n    only_new : bool\n        Whether only new values should be kept. Default is False.\n    fill_missing : bool, optional\n        Whether to impute missing variables with NaN in variable-unit\n        groups.\n    var_cols : str | list[str] | tuple[str]\n        The name(s) of the varaible column(s) to use. Default is\n        `variable`.\n    value_col : str\n        The name of the value column to use. Default is `value`.\n    unit_col : str\n        The name of the unit column to use. Default is `unit`.\n    kw_assignments : int | float | str | Callable\n        A value to assign to a column via keyword assignments.\n\n    Returns\n    -------\n    pd.DataFrame\n        The new dataframe with the newly calculated variables.\n\n    \"\"\"\n    return self.perform(\n        lambda df: _calculate(df, *expr_assignments, **kw_assignments),\n        only_new=only_new,\n        fill_missing=fill_missing,\n        var_cols=var_cols,\n        value_col=value_col,\n        unit_col=unit_col,\n    )\n</code></pre>"},{"location":"api/#team.team_accessor.explode","title":"<code>explode(fields=None, var_cols='variable', value_col='value', unit_col='unit', field_values=None)</code>","text":"<p>Explode rows with NaN entries.</p> <p>Parameters:</p> Name Type Description Default <code>fields</code> <code>str | list[str] | None</code> <p>The list of fields to explode.</p> <code>None</code> <code>var_cols</code> <code>str or list[str] or tuple[str]</code> <p>Name of column containing the variables as strings. Default is <code>variable</code>. Multiple columns can be defined for advanced pivoting (with multi-level column index), e.g. when pivoting both <code>variable</code> and <code>period</code>.</p> <code>'variable'</code> <code>value_col</code> <code>str</code> <p>Name of the column containing the value. Default is <code>value</code>.</p> <code>'value'</code> <code>unit_col</code> <code>str</code> <p>Name of the column containing the unit. Default is <code>unit</code>.</p> <code>'unit'</code> <code>field_values</code> <code>DataFrame</code> <p>Field values to use. By default, the field values will be taken from existing data.</p> <code>None</code> <p>Returns:</p> Type Description <code>    pd.DataFrame</code> <p>The dataframe with nan entries in the respective fields exploded.</p> Source code in <code>team/_accessor.py</code> <pre><code>def explode(\n    self,\n    fields: Optional[str | list[str]] = None,\n    var_cols: str | list[str] | tuple[str] = \"variable\",\n    value_col: str = \"value\",\n    unit_col: str = \"unit\",\n    field_values: pd.DataFrame | None = None,\n) -&gt; pd.DataFrame:\n    \"\"\"Explode rows with NaN entries.\n\n    Parameters\n    ----------\n    fields : str | list[str] | None\n        The list of fields to explode.\n    var_cols : str or list[str] or tuple[str], optional\n        Name of column containing the variables as strings. Default is\n        `variable`. Multiple columns can be defined for advanced pivoting\n        (with multi-level column index), e.g. when pivoting both\n        `variable` and `period`.\n    value_col : str, optional\n        Name of the column containing the value. Default is `value`.\n    unit_col : str, optional\n        Name of the column containing the unit. Default is `unit`.\n    field_values : pd.DataFrame, optional\n        Field values to use. By default, the field values will be taken\n        from existing data.\n\n    Returns\n    -------\n        pd.DataFrame\n            The dataframe with nan entries in the respective fields\n            exploded.\n\n    \"\"\"\n    df = self._df\n\n    if fields is None:\n        fields = self.fields(\n            var_cols=var_cols,\n            value_col=value_col,\n            unit_col=unit_col,\n        )\n    else:\n        if isinstance(fields, str):\n            fields = [fields]\n        for field_id in fields:\n            if field_id not in df.columns:\n                df = df.assign(**{field_id: np.nan})\n\n    if field_values is not None:\n        if not set(fields).issubset(field_values.columns):\n            raise Exception(\n                f\"field_values must contain all field columns. \"\n                f\"Looking for `{','.join(fields)}` but found \"\n                f\"`{','.join(field_values.columns.tolist())}`\"\n            )\n        # Only keep the relevant columns\n        field_values = field_values[fields]\n        if field_values.duplicated().any():\n            raise Exception(\n                \"field_values may not contain duplicates:\\n\"\n                + str(field_values)\n            )\n        if field_values.isnull().any().any():\n            raise Exception(\n                \"field_values may not contain NaNs:\\n\" + str(field_values)\n            )\n\n    if not fields:\n        return df\n\n    # Derive allowed combinations once.\n    if field_values is not None:\n        allowed = (\n            field_values[fields].drop_duplicates().reset_index(drop=True)\n        )\n    else:\n        allowed = _get_allowed_combinations(df, fields)\n\n    # Explode each column to all its unique non-NaN values\n    for field_id in fields:\n        all_values = df[field_id].dropna().unique().tolist()\n        df = df.assign(\n            **{\n                field_id: pd.Series(\n                    len(df) * [all_values], index=df.index\n                ).where(df[field_id].isnull(), other=df[field_id])\n            }\n        )\n        df = df.explode(field_id)\n\n    return df.merge(allowed, on=fields, how=\"inner\").reset_index(drop=True)\n</code></pre>"},{"location":"api/#team.team_accessor.fields","title":"<code>fields(var_cols='variable', value_col='value', unit_col='unit')</code>","text":"<p>Return list of column names of fields.</p> <p>Parameters:</p> Name Type Description Default <code>var_cols</code> <code>str or list[str] or tuple[str]</code> <p>Name of column containing the variables as strings. Default is <code>variable</code>. Multiple columns can be defined for advanced pivotting (with multi-level column index), e.g. when pivotting both <code>variable</code> and <code>period</code>.</p> <code>'variable'</code> <code>value_col</code> <code>str</code> <p>Name of the column containing the value. Default is <code>value</code>.</p> <code>'value'</code> <code>unit_col</code> <code>str</code> <p>Name of the column containing the unit. Default is <code>unit</code>.</p> <code>'unit'</code> <p>Returns:</p> Type Description <code>    list of str</code> <p>A list of strings corresponding to the column names.</p> Source code in <code>team/_accessor.py</code> <pre><code>def fields(\n    self,\n    var_cols: str | list[str] | tuple[str] = \"variable\",\n    value_col: str = \"value\",\n    unit_col: str = \"unit\",\n) -&gt; list[str]:\n    \"\"\"Return list of column names of fields.\n\n    Parameters\n    ----------\n    var_cols : str or list[str] or tuple[str], optional\n        Name of column containing the variables as strings. Default is\n        `variable`. Multiple columns can be defined for advanced pivotting\n        (with multi-level column index), e.g. when pivotting both\n        `variable` and `period`.\n    value_col : str, optional\n        Name of the column containing the value. Default is `value`.\n    unit_col : str, optional\n        Name of the column containing the unit. Default is `unit`.\n\n    Returns\n    -------\n        list of str\n            A list of strings corresponding to the column names.\n\n    \"\"\"\n    if isinstance(var_cols, str):\n        var_cols = [var_cols]\n    return [\n        c for c in self._df if c not in ([value_col, unit_col] + var_cols)\n    ]\n</code></pre>"},{"location":"api/#team.team_accessor.groupby_fields","title":"<code>groupby_fields(fields=None, var_cols='variable', value_col='value', unit_col='unit', **kwargs)</code>","text":"<p>Group by fields columns.</p> <p>Group by field columns (region, period, other...). Fields with rows that contain nan entries will be 'exploded' first. Fields can be passed explicitly or inferred from the column names of the dataframe that are not the variable, unit, or value columns.</p> <p>Parameters:</p> Name Type Description Default <code>fields</code> <code>str | list[str]</code> <p>Single field or list of fields to group by.</p> <code>None</code> <code>var_cols</code> <code>str or list[str] or tuple[str]</code> <p>Name of column containing the variables as strings. Default is <code>variable</code>. Multiple columns can be defined for advanced pivotting (with multi-level column index), e.g. when pivotting both <code>variable</code> and <code>period</code>.</p> <code>'variable'</code> <code>value_col</code> <code>str</code> <p>Name of the column containing the value. Default is <code>value</code>.</p> <code>'value'</code> <code>unit_col</code> <code>str</code> <p>Name of the column containing the unit. Default is <code>unit</code>.</p> <code>'unit'</code> <code>kwargs</code> <p>Passed on to pd.DataFrame.groupby.</p> <code>{}</code> <p>Returns:</p> Type Description <code>    pd.DataFrameGroupBy</code> <p>The grouped dataframe rows.</p> Source code in <code>team/_accessor.py</code> <pre><code>def groupby_fields(\n    self,\n    fields: Optional[str | list[str]] = None,\n    var_cols: str | list[str] | tuple[str] = \"variable\",\n    value_col: str = \"value\",\n    unit_col: str = \"unit\",\n    **kwargs,\n) -&gt; DataFrameGroupBy:\n    \"\"\"Group by fields columns.\n\n    Group by field columns (region, period, other...). Fields with rows\n    that contain nan entries will be 'exploded' first. Fields can be\n    passed explicitly or inferred from the column names of the dataframe\n    that are not the variable, unit, or value columns.\n\n    Parameters\n    ----------\n    fields : str | list[str], optional\n        Single field or list of fields to group by.\n    var_cols : str or list[str] or tuple[str], optional\n        Name of column containing the variables as strings. Default is\n        `variable`. Multiple columns can be defined for advanced pivotting\n        (with multi-level column index), e.g. when pivotting both\n        `variable` and `period`.\n    value_col : str, optional\n        Name of the column containing the value. Default is `value`.\n    unit_col : str, optional\n        Name of the column containing the unit. Default is `unit`.\n    kwargs\n        Passed on to pd.DataFrame.groupby.\n\n    Returns\n    -------\n        pd.DataFrameGroupBy\n            The grouped dataframe rows.\n\n    \"\"\"\n    if \"by\" in kwargs:\n        raise Exception(\n            \"Cannot use 'by' argument. Please use `fields` arugment.\"\n        )\n    if fields is None:\n        fields = self.fields(\n            var_cols=var_cols,\n            value_col=value_col,\n            unit_col=unit_col,\n        )\n    return self.explode(fields=fields).groupby(by=fields, **kwargs)\n</code></pre>"},{"location":"api/#team.team_accessor.perform","title":"<code>perform(func, pre_mapper=None, post_mapper=None, using=None, only_new=False, fill_missing=False, dropna=True, var_cols='variable', value_col='value', unit_col='unit', args=(), **kwargs)</code>","text":"<p>Perform a manipulation by applying a function to the dataframe.</p> <p>Parameters:</p> Name Type Description Default <code>func</code> <code>Callable</code> <p>Function to apply to dataframe. The function should accept a dataframe and return a dataframe. Additional arguments can be supplied through the key-word arguments.</p> required <code>pre_mapper</code> <code>dict - like or function</code> <p>Dict-like or function transformations to apply to column names before passing dataframe as input to <code>func</code>.</p> <code>None</code> <code>post_mapper</code> <code>dict - like or function</code> <p>Dict-like or function transformations to apply to column names before processing output from <code>func</code>.</p> <code>None</code> <code>only_new</code> <code>bool</code> <p>Whether to only keep new variables. Default is False.</p> <code>False</code> <code>fill_missing</code> <code>bool</code> <p>Whether to impute missing variables with NaN in variable-unit groups.</p> <code>False</code> <code>using</code> <code>DataFrame or list[DataFrame]</code> <p>Data/assumptions to use but not append to the output dataframe.</p> <code>None</code> <code>dropna</code> <code>bool</code> <p>Whether to drop NaN entries resulting from the function call. Default is True.</p> <code>True</code> <code>var_cols</code> <code>str or list[str] or tuple[str]</code> <p>Name of column containing the variables as strings. Default is <code>variable</code>. Multiple columns can be defined for advanced pivotting (with multi-level column index), e.g. when pivotting both <code>variable</code> and <code>period</code>.</p> <code>'variable'</code> <code>value_col</code> <code>str</code> <p>Name of the column containing the value. Default is <code>value</code>.</p> <code>'value'</code> <code>unit_col</code> <code>str</code> <p>Name of the column containing the unit. Default is <code>unit</code>.</p> <code>'unit'</code> <code>args</code> <code>tuple</code> <p>Positional arguments to pass to <code>func</code> in addition to the data contained in the dataframe.</p> <code>()</code> <code>**kwargs</code> <p>Additional keyword arguments to pass to <code>func</code> in addition to the data contained in the dataframe.</p> <code>{}</code> <p>Returns:</p> Type Description <code>    pd.DataFrame</code> <p>The dataframe that underwent the manipulation.</p> Source code in <code>team/_accessor.py</code> <pre><code>def perform(\n    self,\n    func: Callable,\n    pre_mapper: dict | Callable | None = None,\n    post_mapper: dict | Callable | None = None,\n    using: pd.DataFrame | list[pd.DataFrame] | None = None,\n    only_new: bool = False,\n    fill_missing: bool = False,\n    dropna: bool = True,\n    var_cols: str | list[str] | tuple[str] = \"variable\",\n    value_col: str = \"value\",\n    unit_col: str = \"unit\",\n    args: tuple = (),\n    **kwargs,\n):\n    \"\"\"Perform a manipulation by applying a function to the dataframe.\n\n    Parameters\n    ----------\n    func : Callable\n        Function to apply to dataframe. The function should accept a\n        dataframe and return a dataframe. Additional arguments can be\n        supplied through the key-word arguments.\n    pre_mapper : dict-like or function, optional\n        Dict-like or function transformations to apply to column names\n        before passing dataframe as input to `func`.\n    post_mapper : dict-like or function, optional\n        Dict-like or function transformations to apply to column names\n        before processing output from `func`.\n    only_new : bool, optional\n        Whether to only keep new variables. Default is False.\n    fill_missing : bool, optional\n        Whether to impute missing variables with NaN in variable-unit\n        groups.\n    using : pd.DataFrame or list[pd.DataFrame], optional\n        Data/assumptions to use but not append to the output dataframe.\n    dropna: bool, optional\n        Whether to drop NaN entries resulting from the function call.\n        Default is True.\n    var_cols : str or list[str] or tuple[str], optional\n        Name of column containing the variables as strings. Default is\n        `variable`. Multiple columns can be defined for advanced pivotting\n        (with multi-level column index), e.g. when pivotting both\n        `variable` and `period`.\n    value_col : str, optional\n        Name of the column containing the value. Default is `value`.\n    unit_col : str, optional\n        Name of the column containing the unit. Default is `unit`.\n    args : tuple\n        Positional arguments to pass to `func` in addition to the data\n        contained in the dataframe.\n    **kwargs\n        Additional keyword arguments to pass to `func` in addition to the\n        data contained in the dataframe.\n\n    Returns\n    -------\n        pd.DataFrame\n            The dataframe that underwent the manipulation.\n\n    \"\"\"\n    return self.perform_multi(\n        [\n            dict(\n                func=func,\n                pre_mapper=pre_mapper,\n                post_mapper=post_mapper,\n                args=args,\n                **kwargs,\n            )\n        ],\n        using=using,\n        only_new=only_new,\n        fill_missing=fill_missing,\n        dropna=dropna,\n        var_cols=var_cols,\n        value_col=value_col,\n        unit_col=unit_col,\n    )\n</code></pre>"},{"location":"api/#team.team_accessor.perform_multi","title":"<code>perform_multi(funcs, using=None, only_new=False, dropna=True, fill_missing=False, var_cols='variable', value_col='value', unit_col='unit')</code>","text":"<p>Perform multiple manipulations to a dataframe at once.</p> <p>Parameters:</p> Name Type Description Default <code>funcs</code> <code>list[dict]</code> <p>A list of dictionaries, where each dictionary contains arguments <code>func</code> and (optionally) <code>pre_mapper</code>, <code>post_mapper</code>, <code>only_new</code>, <code>dropna</code>, <code>args</code>, and <code>kwargs</code> (same as <code>.team.perform()</code>).</p> required <code>only_new</code> <code>bool</code> <p>Whether to only keep new variables. Default is False.</p> <code>False</code> <code>fill_missing</code> <code>bool</code> <p>Whether to impute missing variables with NaN in variable-unit groups.</p> <code>False</code> <code>using</code> <code>DataFrame or list[DataFrame]</code> <p>Data/assumptions to use but not append to the output dataframe.</p> <code>None</code> <code>dropna</code> <code>bool</code> <p>Whether to drop NaN entries resulting from the function call. Default is True.</p> <code>True</code> <code>var_cols</code> <code>str or list[str] or tuple[str]</code> <p>Name of column containing the variables as strings. Default is <code>variable</code>. Multiple columns can be defined for advanced pivotting (with multi-level column index), e.g. when pivotting both <code>variable</code> and <code>period</code>.</p> <code>'variable'</code> <code>value_col</code> <code>str</code> <p>Name of the column containing the value. Default is <code>value</code>.</p> <code>'value'</code> <code>unit_col</code> <code>str</code> <p>Name of the column containing the unit. Default is <code>unit</code>.</p> <code>'unit'</code> <p>Returns:</p> Type Description <code>    pd.DataFrame</code> <p>The dataframe that underwent the manipulation(s).</p> Source code in <code>team/_accessor.py</code> <pre><code>def perform_multi(\n    self,\n    funcs: list[dict],\n    using: pd.DataFrame | list[pd.DataFrame] | None = None,\n    only_new: bool = False,\n    dropna: bool = True,\n    fill_missing: bool = False,\n    var_cols: str | list[str] | tuple[str] = \"variable\",\n    value_col: str = \"value\",\n    unit_col: str = \"unit\",\n):\n    \"\"\"Perform multiple manipulations to a dataframe at once.\n\n    Parameters\n    ----------\n    funcs : list[dict]\n        A list of dictionaries, where each dictionary contains arguments\n        `func` and (optionally) `pre_mapper`, `post_mapper`, `only_new`,\n        `dropna`, `args`, and `kwargs` (same as `.team.perform()`).\n    only_new : bool, optional\n        Whether to only keep new variables. Default is False.\n    fill_missing : bool, optional\n        Whether to impute missing variables with NaN in variable-unit\n        groups.\n    using : pd.DataFrame or list[pd.DataFrame], optional\n        Data/assumptions to use but not append to the output dataframe.\n    dropna: bool, optional\n        Whether to drop NaN entries resulting from the function call.\n        Default is True.\n    var_cols : str or list[str] or tuple[str], optional\n        Name of column containing the variables as strings. Default is\n        `variable`. Multiple columns can be defined for advanced pivotting\n        (with multi-level column index), e.g. when pivotting both\n        `variable` and `period`.\n    value_col : str, optional\n        Name of the column containing the value. Default is `value`.\n    unit_col : str, optional\n        Name of the column containing the unit. Default is `unit`.\n\n    Returns\n    -------\n        pd.DataFrame\n            The dataframe that underwent the manipulation(s).\n\n    \"\"\"\n    # Convert var_cols to list if tuple.\n    if isinstance(var_cols, tuple):\n        var_cols = list(var_cols)\n    elif not isinstance(var_cols, list):\n        var_cols = [var_cols]\n\n    # The old rows are the original dataframe.\n    df_old = self._df\n\n    # Depending on the arguments, we may combine this with `using` to\n    # a new dataframe called df_old_full.\n    if using is not None:\n        if isinstance(using, list | tuple) and all(\n            isinstance(u, pd.DataFrame) for u in using\n        ):\n            using = pd.concat(using)\n        elif not isinstance(using, pd.DataFrame):\n            raise Exception(\n                \"Argument `using` must be dataframe or list of dataframes.\"\n            )\n\n        df_old_full = pd.concat([df_old, using])\n    else:\n        df_old_full = df_old\n\n    # Explode.\n    fields = df_old_full.team.fields(\n        var_cols=var_cols,\n        unit_col=unit_col,\n        value_col=value_col,\n    )\n    df_old_full = TEAMAccessor(df_old_full).explode(fields=fields)\n\n    # Create dummy field if none exists.\n    if not fields:\n        fields = [\"unfielded\"]\n        df_old_full = df_old_full.assign(unfielded=0)\n\n    # Group by all fields, by variable columns, and by unit. Verify\n    # integrity to ensure that there are no duplicates.\n    df_old_full = df_old_full.set_index(\n        fields + var_cols + [unit_col], verify_integrity=True\n    )\n\n    # Pivot dataframe and set pint units.\n    df_pivot = df_old_full[value_col].unstack(var_cols + [unit_col])\n\n    # Get a presence matrix.\n    presence = pd.Series(True, index=df_old_full.index).unstack(\n        var_cols + [unit_col], fill_value=False\n    )\n\n    # Determine presence groups.\n    pivot_groups = presence.groupby([c for c in presence.columns]).groups\n\n    # Loop over pivot groups.\n    return_list = []\n    for pivot_group_cols, pivot_group_row in pivot_groups.items():\n        # Get column names from boolean indexer.\n        pivot_group_cols = df_pivot.columns[\n            pd.Series(pivot_group_cols, index=df_pivot.columns)\n        ]\n\n        # Make sure that there are no columns with more than one unit\n        # defined per variable.\n        if not pivot_group_cols.droplevel(level=unit_col).is_unique:\n            raise Exception(\n                \"Variable defined with multiple units: \"\n                + df_pivot.loc[pivot_group_row]\n            )\n\n        # Get the pivot group by selecting relevant rows and columns and\n        # using pint to set the dtype of the column from the unit level.\n        df_pivot_group = df_pivot.loc[\n            pivot_group_row, pivot_group_cols\n        ].pint.quantify()\n\n        # Keep a copy of the index to ensure that it is not changed.\n        original_index = df_pivot_group.index\n\n        # Insert missing columns. This will be necessary in case the\n        # performed actions operate on column not present in this group.\n        if fill_missing:\n            cols = (\n                df_pivot.columns.to_frame()\n                .droplevel(unit_col)[unit_col]\n                .to_dict()\n            )\n            missing_cols = {\n                col_id: col_unit\n                for col_id, col_unit in cols.items()\n                if col_id not in df_pivot_group.columns\n            }\n            for col_id, col_unit in missing_cols.items():\n                df_pivot_group[col_id] = pd.Series(\n                    np.nan, dtype=f\"pint[{col_unit}][float64]\"\n                )\n\n        # Apply functions.\n        for func_specs in funcs:\n            # Fetch from dict.\n            func = func_specs.get(\"func\")\n            pre_mapper = func_specs.get(\"pre_mapper\")\n            post_mapper = func_specs.get(\"post_mapper\")\n            args = func_specs.get(\"args\") or ()\n            kwargs = {\n                k: v\n                for k, v in func_specs.items()\n                if k not in [\"func\", \"pre_mapper\", \"post_mapper\", \"args\"]\n            }\n\n            # Raise Exception if func not provided.\n            if func is None:\n                raise Exception(\"No function provided to call.\")\n            elif not isinstance(func, Callable):\n                raise Exception(\"Function provided must be callable.\")\n\n            # Prepare data for function call.\n            df_pivot_group_mapped = (\n                (df_pivot_group.rename(columns=pre_mapper))\n                if pre_mapper is not None\n                else df_pivot_group\n            )\n\n            # Call function.\n            df_pivot_group_ret = func(\n                df_pivot_group_mapped, *args, **kwargs\n            )\n\n            # Check results. Skip if result is empty.\n            if df_pivot_group_ret is None:\n                continue\n            elif isinstance(df_pivot_group_ret, dict):\n                df_pivot_group_ret = pd.DataFrame(df_pivot_group_ret)\n            elif not isinstance(df_pivot_group_ret, pd.DataFrame):\n                raise Exception(\"Return type must be a dict or dataframe.\")\n            if (\n                df_pivot_group_ret.empty\n                or df_pivot_group_ret.isnull().all().all()\n            ):\n                continue\n            if not df_pivot_group_ret.index.equals(original_index):\n                raise Exception(\"Manipulation may not change the index.\")\n\n            # Postprocess results and add to list.\n            if post_mapper is not None:\n                df_pivot_group_ret = df_pivot_group_ret.rename(\n                    columns=post_mapper\n                )\n\n            # Drop dummy columns.\n            if fill_missing and missing_cols:\n                df_pivot_group_ret.drop(\n                    columns=list(missing_cols),\n                    errors=\"ignore\",\n                    inplace=True,\n                )\n\n            # Combine results from functions, melt, and append to list.\n            return_list.append(\n                df_pivot_group_ret\n                # Ensure that the axis label still exists before melt.\n                .rename_axis(var_cols, axis=1)\n                # Split units off into separate column.\n                .pint.dequantify()\n                # Melt.\n                .melt(ignore_index=False)\n                # Append var and unit as index.\n                .set_index(var_cols + [unit_col], append=True)\n            )\n\n    # Check whether return list is empty.\n    if not return_list:\n        if only_new:\n            return pd.DataFrame(columns=df_old.columns)\n        else:\n            return df_old\n\n    # Combine groups into single dataframe.\n    df_new = pd.concat(return_list)\n\n    # Convert pint objects to unit string and float value if any.\n    if df_new[value_col].dtype == \"object\":\n        df_new = df_new.reset_index(level=unit_col)\n        locs = df_new[value_col].apply(lambda x: isinstance(x, Quantity))\n        df_new.loc[locs, [unit_col, value_col]] = df_new.loc[\n            locs, value_col\n        ].apply(lambda x: pd.Series({unit_col: x.u, value_col: x.m}))\n        df_new = df_new.set_index(unit_col, append=True)\n\n    # Drop rows with nan entries in unit or value columns.\n    if dropna:\n        df_new.dropna(subset=value_col, inplace=True)\n\n    # Drop rows that are already in old.\n    common_idx = df_new.index.intersection(df_old_full.index)\n    same = (df_new.loc[common_idx] == df_old_full.loc[common_idx]).all(\n        axis=1\n    )\n    df_new = df_new.drop(common_idx[same])\n\n    # Raise exception if index has duplicates after the above.\n    if not df_new.index.is_unique:\n        duplicate_labels = df_new.loc[\n            df_new.index.duplicated(), fields + var_cols\n        ]\n        warnings.warn(\n            f\"Internal error: variables should only exist \"\n            f\"once per case: {duplicate_labels}\"\n        )\n\n    # Drop column called 'unfielded' if it exists.\n    if \"unfielded\" in df_new.index.names:\n        df_new = df_new.droplevel(\"unfielded\")\n\n    # Combine with old if requested.\n    if only_new:\n        ret = df_new\n    else:\n        df_old_extended_index = df_old.set_index(\n            [c for c in df_new.index.names if c in df_old.columns]\n        )\n        for level in df_new.index.names:\n            if level not in df_old_extended_index.index.names:\n                df_old_extended_index = df_old_extended_index.set_index(\n                    pd.Index(\n                        [np.nan] * len(df_old_extended_index),\n                        name=level,\n                    ),\n                    append=True,\n                )\n        ret = df_new.reset_index(level=unit_col).combine_first(\n            df_old_extended_index.reset_index(level=unit_col)\n        )\n\n    # Return.\n    return ret.reset_index()\n</code></pre>"},{"location":"api/#team.team_accessor.pivot_wide","title":"<code>pivot_wide(var_cols='variable', value_col='value', unit_col='unit')</code>","text":"<p>Pivot dataframe wide, such that column names are variables.</p> <p>Parameters:</p> Name Type Description Default <code>var_cols</code> <code>str or list[str] or tuple[str]</code> <p>Name of column containing the variables as strings. Default is <code>variable</code>. Multiple columns can be defined for advanced pivotting (with multi-level column index), e.g. when pivotting both <code>variable</code> and <code>period</code>.</p> <code>'variable'</code> <code>value_col</code> <code>str</code> <p>Name of the column containing the value. Default is <code>value</code>.</p> <code>'value'</code> <code>unit_col</code> <code>str</code> <p>Name of the column containing the unit. Default is <code>unit</code>.</p> <code>'unit'</code> <p>Returns:</p> Type Description <code>    pd.DataFrame</code> <p>The original dataframe in pivot mode.</p> Source code in <code>team/_accessor.py</code> <pre><code>def pivot_wide(\n    self,\n    var_cols: str | list[str] | tuple[str] = \"variable\",\n    value_col: str = \"value\",\n    unit_col: str = \"unit\",\n):\n    \"\"\"Pivot dataframe wide, such that column names are variables.\n\n    Parameters\n    ----------\n    var_cols : str or list[str] or tuple[str], optional\n        Name of column containing the variables as strings. Default is\n        `variable`. Multiple columns can be defined for advanced pivotting\n        (with multi-level column index), e.g. when pivotting both\n        `variable` and `period`.\n    value_col : str, optional\n        Name of the column containing the value. Default is `value`.\n    unit_col : str, optional\n        Name of the column containing the unit. Default is `unit`.\n\n    Returns\n    -------\n        pd.DataFrame\n            The original dataframe in pivot mode.\n\n    \"\"\"\n    fields = self.fields(\n        var_cols=var_cols,\n        value_col=value_col,\n        unit_col=unit_col,\n    )\n    ret = self.explode(\n        fields=fields,\n    )\n\n    # Create dummy field if non exists.\n    if not fields:\n        ret = ret.assign(unfielded=0)\n        fields = [\"unfielded\"]\n\n    # Set variable columns.\n    if isinstance(var_cols, str):\n        var_cols = [var_cols]\n\n    # Pivot dataframe.\n    ret = ret.pivot(\n        index=fields,\n        columns=var_cols + [unit_col],\n        values=value_col,\n    )\n\n    # Raise exception if duplicate cases exist.\n    if ret.index.has_duplicates:\n        raise ValueError(\n            \"Performed pivot_wide on dataframe with \"\n            \"duplicate cases. Each variable should only be \"\n            \"defined once for each combination of field \"\n            \"values.\"\n        )\n\n    return ret.pint.quantify()\n</code></pre>"},{"location":"api/#team.team_accessor.sort_columns","title":"<code>sort_columns(fields_order=None, var_cols='variable', value_col='value', unit_col='unit')</code>","text":"<p>Sort columns of a dataframe based on the TEAM logic.</p> <p>This will sort fields in the beginning, followed by variable, unit, and value columns. By passing <code>fields_order</code>, the order for the fields can be manually set.</p> <p>Parameters:</p> Name Type Description Default <code>fields_order</code> <code>list[str]</code> <p>Option for manually defining the order of field columns that come before the variable, unit, and value columns.</p> <code>None</code> <code>var_cols</code> <code>str or list[str] | tuple[str]</code> <p>Option to override name of the variable columns. Default is <code>variable</code>.</p> <code>'variable'</code> <code>value_col</code> <code>str</code> <p>Option to override name of the value column. Default is <code>value</code>.</p> <code>'value'</code> <code>unit_col</code> <code>str</code> <p>Option to override name of the unit column. Default is <code>unit</code>.</p> <code>'unit'</code> Source code in <code>team/_accessor.py</code> <pre><code>def sort_columns(\n    self,\n    fields_order: list[str] | None = None,\n    var_cols: str | list[str] | tuple[str] = \"variable\",\n    value_col: str = \"value\",\n    unit_col: str = \"unit\",\n) -&gt; pd.DataFrame:\n    \"\"\"Sort columns of a dataframe based on the TEAM logic.\n\n    This will sort fields in the beginning, followed by variable, unit,\n    and value columns. By passing `fields_order`, the order for the fields\n    can be manually set.\n\n    Parameters\n    ----------\n    fields_order : list[str], optional\n        Option for manually defining the order of field columns that come\n        before the variable, unit, and value columns.\n    var_cols : str or list[str] | tuple[str], optional\n        Option to override name of the variable columns. Default is\n        `variable`.\n    value_col : str, optional\n        Option to override name of the value column. Default is `value`.\n    unit_col : str, optional\n        Option to override name of the unit column. Default is `unit`.\n\n    \"\"\"\n    return _sort_cols(\n        df=self._df,\n        fields_order=fields_order or [],\n        var_cols=(\n            [var_cols] if isinstance(var_cols, str) else list(var_cols)\n        ),\n        value_col=value_col,\n        unit_col=unit_col,\n    )\n</code></pre>"},{"location":"api/#team.team_accessor.sort_rows","title":"<code>sort_rows(by=None, var_cols='variable', value_col='value', unit_col='unit')</code>","text":"<p>Sort rows of a dataframe based on the TEAM logic.</p> <p>This will sort rows either by the columns provided by argument <code>by</code> or, if no <code>by</code> is provided, by all fields and the variable column.</p> <p>Parameters:</p> Name Type Description Default <code>by</code> <code>str | list[str]</code> <p>Option for manually defining the columns by which to sort.</p> <code>None</code> <code>var_cols</code> <code>str or list[str] | tuple[str]</code> <p>Option to override name of the variable columns. Default is <code>variable</code>.</p> <code>'variable'</code> <code>value_col</code> <code>str</code> <p>Option to override name of the value column. Default is <code>value</code>.</p> <code>'value'</code> <code>unit_col</code> <code>str</code> <p>Option to override name of the unit column. Default is <code>unit</code>.</p> <code>'unit'</code> Source code in <code>team/_accessor.py</code> <pre><code>def sort_rows(\n    self,\n    by: str | list[str] | None = None,\n    var_cols: str | list[str] | tuple[str] = \"variable\",\n    value_col: str = \"value\",\n    unit_col: str = \"unit\",\n):\n    \"\"\"Sort rows of a dataframe based on the TEAM logic.\n\n    This will sort rows either by the columns provided by argument `by` or,\n    if no `by` is provided, by all fields and the variable column.\n\n    Parameters\n    ----------\n    by : str | list[str], optional\n        Option for manually defining the columns by which to sort.\n    var_cols : str or list[str] | tuple[str], optional\n        Option to override name of the variable columns. Default is\n        `variable`.\n    value_col : str, optional\n        Option to override name of the value column. Default is `value`.\n    unit_col : str, optional\n        Option to override name of the unit column. Default is `unit`.\n\n    \"\"\"\n    if by is None:\n        var_cols = (\n            [var_cols] if isinstance(var_cols, str) else list(var_cols)\n        )\n        base_cols = var_cols + [value_col, unit_col]\n\n        by = [c for c in self._df if c not in base_cols]\n        if all(v in self._df for v in var_cols):\n            by += var_cols\n    elif not by:\n        raise Exception(\"Argument `by` may not be an empty list or str.\")\n\n    return self._df.sort_values(by=by).reset_index(drop=True)\n</code></pre>"},{"location":"api/#team.team_accessor.sum_over","title":"<code>sum_over(over, value_col='value', unit_col='unit')</code>","text":"<p>Sum up values over specified fields.</p> <p>Parameters:</p> Name Type Description Default <code>over</code> <code>str | list[str] | tuple[str]</code> <p>The name of the field (column) to sum over.</p> required <code>value_col</code> <code>str</code> <p>Name of the column containing the value. Default is <code>value</code>.</p> <code>'value'</code> <code>unit_col</code> <code>str</code> <p>Name of the column containing the unit. Default is <code>unit</code>.</p> <code>'unit'</code> <p>Returns:</p> Type Description <code>    pd.DataFrame</code> <p>The aggregated dataframe.</p> Source code in <code>team/_accessor.py</code> <pre><code>def sum_over(\n    self,\n    over: str | list[str] | tuple[str],\n    value_col: str = \"value\",\n    unit_col: str = \"unit\",\n):\n    \"\"\"Sum up values over specified fields.\n\n    Parameters\n    ----------\n    over : str | list[str] | tuple[str]\n        The name of the field (column) to sum over.\n    value_col : str, optional\n        Name of the column containing the value. Default is `value`.\n    unit_col : str, optional\n        Name of the column containing the unit. Default is `unit`.\n\n    Returns\n    -------\n        pd.DataFrame\n            The aggregated dataframe.\n\n    \"\"\"\n    # Convert single string to list of strings.\n    if isinstance(over, str):\n        over = [over]\n\n    def _helper_sum_units(group):\n        q = (\n            group.groupby(unit_col)\n            .agg(\"sum\")\n            .reset_index()\n            .apply(\n                lambda row: row[value_col] * Unit(row[unit_col]), axis=1\n            )\n            .sum()\n        )\n        return pd.Series(\n            {\n                value_col: q.m,\n                unit_col: f\"{q.u:~}\",\n            }\n        )\n\n    group_cols = [\n        c\n        for c in self._df\n        if c not in over and c not in [value_col, unit_col]\n    ]\n\n    if not group_cols:\n        # implement\n        pass\n\n    return (\n        self._df\n        # group by all columns except unit, value, and 'over'\n        .groupby(group_cols, dropna=False)[\n            # select only unit and value columns\n            [unit_col, value_col]\n        ]\n        # apply summation\n        .apply(\n            lambda group: (\n                _helper_sum_units(group)\n                if group[unit_col].nunique() &gt; 1\n                else pd.Series(\n                    {\n                        value_col: group[value_col].sum(),\n                        unit_col: group[unit_col].iloc[0],\n                    }\n                )\n            )\n        )\n        .reset_index()\n    )\n</code></pre>"},{"location":"api/#team.team_accessor.unit_to","title":"<code>unit_to(other, *contexts, var_cols='variable', value_col='value', unit_col='unit', **ctx_kwargs)</code>","text":"<p>Convert units in dataframe.</p> <p>Parameters:</p> Name Type Description Default <code>other</code> <code>str | Unit | dict[str, str | Unit]</code> <p>The unit to convert to. This is either one unit for all rows or a dict that maps variables to units.</p> required <code>contexts</code> <code>str | Context</code> <p>A pint unit context to assume for unit conversion.</p> <code>()</code> <code>var_cols</code> <code>str | list[str] | tuple[str]</code> <p>The name(s) of the varaible column(s) to use. Default is <code>variable</code>.</p> <code>'variable'</code> <code>value_col</code> <code>str</code> <p>The name of the value column to use. Default is <code>value</code>.</p> <code>'value'</code> <code>unit_col</code> <code>str</code> <p>The name of the unit column to use. Default is <code>unit</code>.</p> <code>'unit'</code> <code>ctx_kwargs</code> <code>float</code> <p>Constants or parameters to assume as part of the unit context.</p> <code>{}</code> <p>Returns:</p> Type Description <code>    pd.DataFrame</code> <p>The dataframe with updated units.</p> Source code in <code>team/_accessor.py</code> <pre><code>def unit_to(\n    self,\n    other: str | Unit | dict[str, str | Unit],\n    *contexts: str | pint.Context,\n    var_cols: str | list[str] | tuple[str] = \"variable\",\n    value_col: str = \"value\",\n    unit_col: str = \"unit\",\n    **ctx_kwargs,\n):\n    \"\"\"Convert units in dataframe.\n\n    Parameters\n    ----------\n    other : str | team_units.Unit | dict[str, str | team_units.Unit]\n        The unit to convert to. This is either one unit for all rows\n        or a dict that maps variables to units.\n    contexts : str | pint.Context\n        A pint unit context to assume for unit conversion.\n    var_cols : str | list[str] | tuple[str]\n        The name(s) of the varaible column(s) to use. Default is\n        `variable`.\n    value_col : str\n        The name of the value column to use. Default is `value`.\n    unit_col : str\n        The name of the unit column to use. Default is `unit`.\n    ctx_kwargs : float\n        Constants or parameters to assume as part of the unit context.\n\n    Returns\n    -------\n        pd.DataFrame\n            The dataframe with updated units.\n\n    \"\"\"\n    return self.__unit_convert(\n        \"to\",\n        other=other,\n        contexts=contexts,\n        var_cols=var_cols,\n        value_col=value_col,\n        unit_col=unit_col,\n        ctx_kwargs=ctx_kwargs,\n    )\n</code></pre>"},{"location":"api/#team.team_accessor.unit_to_preferred","title":"<code>unit_to_preferred(other, *contexts, var_cols='variable', value_col='value', unit_col='unit', **ctx_kwargs)</code>","text":"<p>Convert units in dataframe.</p> <p>Parameters:</p> Name Type Description Default <code>other</code> <code>list[Unit] | dict[str, list[Unit]]</code> <p>The preferred units to use for conversion. This is either one list of units for all rows or a dict that maps variables to lists of units.</p> required <code>contexts</code> <code>str | Context</code> <p>A pint unit context to assume for unit conversion.</p> <code>()</code> <code>var_cols</code> <code>str | list[str] | tuple[str]</code> <p>The name(s) of the varaible column(s) to use. Default is <code>variable</code>.</p> <code>'variable'</code> <code>value_col</code> <code>str</code> <p>The name of the value column to use. Default is <code>value</code>.</p> <code>'value'</code> <code>unit_col</code> <code>str</code> <p>The name of the unit column to use. Default is <code>unit</code>.</p> <code>'unit'</code> <code>ctx_kwargs</code> <p>Constants or parameters to assume as part of the unit context.</p> <code>{}</code> <p>Returns:</p> Type Description <code>    pd.DataFrame</code> <p>The dataframe with updated units.</p> Source code in <code>team/_accessor.py</code> <pre><code>def unit_to_preferred(\n    self,\n    other: list[Unit] | dict[str, list[Unit]],\n    *contexts,\n    var_cols: str | list[str] | tuple[str] = \"variable\",\n    value_col: str = \"value\",\n    unit_col: str = \"unit\",\n    **ctx_kwargs,\n):\n    \"\"\"Convert units in dataframe.\n\n    Parameters\n    ----------\n    other : list[Unit] | dict[str, list[Unit]]\n        The preferred units to use for conversion. This is either one\n        list of units for all rows or a dict that maps variables to\n        lists of units.\n    contexts : str | pint.Context\n        A pint unit context to assume for unit conversion.\n    var_cols : str | list[str] | tuple[str]\n        The name(s) of the varaible column(s) to use. Default is\n        `variable`.\n    value_col : str\n        The name of the value column to use. Default is `value`.\n    unit_col : str\n        The name of the unit column to use. Default is `unit`.\n    ctx_kwargs\n        Constants or parameters to assume as part of the unit context.\n\n    Returns\n    -------\n        pd.DataFrame\n            The dataframe with updated units.\n\n    \"\"\"\n    return self.__unit_convert(\n        \"to_preferred\",\n        other=other,\n        contexts=contexts,\n        var_cols=var_cols,\n        value_col=value_col,\n        unit_col=unit_col,\n        ctx_kwargs=ctx_kwargs,\n    )\n</code></pre>"},{"location":"api/#team.team_accessor.varcombine","title":"<code>varcombine(cmd, keep_cols=False, var_col='variable')</code>","text":"<p>Combine columns into new variable (or other column).</p> <p>Parameters:</p> Name Type Description Default <code>cmd</code> <code>str | Callable</code> <p>How the new variable (or other column) should be assembled.</p> required <code>keep_cols</code> <code>bool</code> <p>Whether to keep the used columns.</p> <code>False</code> <code>var_col</code> <code>str</code> <p>(Optional) The name of the target column. By default, this will be called <code>variable</code>.</p> <code>'variable'</code> <p>Returns:</p> Type Description <code>    pd.DataFrame</code> <p>The updated dataframe.</p> Source code in <code>team/_accessor.py</code> <pre><code>def varcombine(\n    self,\n    cmd: str | Callable,\n    keep_cols: bool = False,\n    var_col: str = \"variable\",\n) -&gt; pd.DataFrame:\n    \"\"\"Combine columns into new variable (or other column).\n\n    Parameters\n    ----------\n    cmd : str | Callable\n        How the new variable (or other column) should be assembled.\n    keep_cols : bool\n        Whether to keep the used columns.\n    var_col : str\n        (Optional) The name of the target column. By default, this\n        will be called `variable`.\n\n    Returns\n    -------\n        pd.DataFrame\n            The updated dataframe.\n\n    \"\"\"\n    ret = self._df.assign(\n        **{\n            var_col: self._df.apply(\n                lambda row: (\n                    cmd.format(**row) if isinstance(cmd, str) else cmd(row)\n                ),\n                axis=1,\n            ),\n        }\n    )\n    return (\n        ret\n        if keep_cols\n        else ret.filter(\n            [\n                col\n                for col in ret\n                if col == var_col\n                or (isinstance(cmd, Callable) or f\"{{{col}}}\" not in cmd)\n            ]\n        )\n    )\n</code></pre>"},{"location":"api/#team.team_accessor.varsplit","title":"<code>varsplit(cmd=None, regex=None, var_col_new='variable', var_new=True, keep_unmatched=False)</code>","text":"<p>Split variable column by pipe characters.</p> <p>Split variable components separated by pipe characters into separate columns. The pattern must either be provided as <code>cmd</code> or as <code>regex</code>.</p> <p>Parameters:</p> Name Type Description Default <code>cmd</code> <code>str</code> <p>A command to interpret into a regex.</p> <code>None</code> <code>regex</code> <code>str</code> <p>A direct regex.</p> <code>None</code> <code>var_col_new</code> <code>str</code> <p>(Optional) The name of the column where the new variable will be stored.</p> <code>'variable'</code> <code>var_new</code> <code>str</code> <p>(Optional) The new variable name. True means, it is computed automatically. A string will be used to override it. False means that it is not added.</p> <code>True</code> <code>keep_unmatched</code> <code>bool</code> <p>Whether or not to keep unmatched rows.</p> <code>False</code> <p>Returns:</p> Type Description <code>    pd.DataFrame</code> <p>The dataframe that contains the new split variables.</p> Source code in <code>team/_accessor.py</code> <pre><code>def varsplit(\n    self,\n    cmd: Optional[str] = None,\n    regex: Optional[str] = None,\n    var_col_new: str = \"variable\",\n    var_new: str | bool = True,\n    keep_unmatched: bool = False,\n) -&gt; pd.DataFrame:\n    \"\"\"Split variable column by pipe characters.\n\n    Split variable components separated by pipe characters into separate\n    columns. The pattern must either be provided as `cmd` or as `regex`.\n\n    Parameters\n    ----------\n    cmd : str\n        A command to interpret into a regex.\n    regex : str\n        A direct regex.\n    var_col_new : str\n        (Optional) The name of the column where the new\n        variable will be stored.\n    var_new : str\n        (Optional) The new variable name. True means, it is computed\n        automatically. A string will be used to override it. False means\n        that it is not added.\n    keep_unmatched : bool\n        Whether or not to keep unmatched rows.\n\n    Returns\n    -------\n        pd.DataFrame\n            The dataframe that contains the new split variables.\n\n    \"\"\"\n    # Check that precisely one of the two arguments (either `cmd`\n    # or `regex`) is provided.\n    if cmd is not None and regex is not None:\n        raise Exception(\n            \"Only one of the two arguments may be provided: cmd or regex.\"\n        )\n    if cmd is None and regex is None:\n        raise Exception(\n            \"Either a command or a regex string must be provided.\"\n        )\n\n    # Check that target is in columns of dataframe.\n    if var_col_new not in self._df.columns:\n        raise Exception(\n            f\"Could not find column of name '{var_col_new}' in dataframe.\"\n        )\n\n    # Determine regex from cmd if necessary.\n    if regex is None:\n        regex = (\n            \"^\"\n            + r\"\\|\".join(\n                [\n                    rf\"(?P&lt;{t[1:]}&gt;[^|]*)\"\n                    if t[0] == \"?\"\n                    else rf\"(?P&lt;{t[1:]}&gt;.*)\"\n                    if t[0] == \"*\"\n                    else escape(t)\n                    for t in cmd.split(\"|\")\n                ]\n            )\n            + \"$\"\n        )\n\n    # Determine value of new variable column from arguments.\n    if var_new is False:\n        var_new = None\n    elif var_new is True:\n        if cmd is None:\n            var_new = None\n        else:\n            var_new = \"|\".join(\n                [t for t in cmd.split(\"|\") if t[0] not in (\"?\", \"*\")]\n            )\n\n    # Create dataframe to be returned by applying regex to variable\n    # column and dropping unmatched rows.\n    matched = self._df[var_col_new].str.extract(regex)\n\n    # Drop unmatched rows if requested.\n    is_unmatched = matched.isna().any(axis=1)\n    matched = matched.drop(index=matched.loc[is_unmatched].index)\n\n    # Assign new variable column and drop if all are nan.\n    if var_col_new not in matched:\n        cond = matched.notnull().any(axis=1)\n        matched[var_col_new] = self._df[var_col_new]\n        matched.loc[cond, var_col_new] = var_new or np.nan\n        if var_new is None:\n            warnings.warn(\n                \"New target column could not be set automatically.\"\n            )\n\n    # Drop variable column if all nan.\n    if matched[var_col_new].isnull().all():\n        matched.drop(columns=var_col_new, inplace=True)\n\n    # Combine with original dataframe.\n    if keep_unmatched:\n        df_combine = self._df.assign(\n            **{var_col_new: lambda df: df[var_col_new].where(is_unmatched)}\n        )\n    else:\n        df_combine = self._df.loc[matched.index].drop(columns=var_col_new)\n    ret = matched.combine_first(df_combine)\n\n    # Sort columns.\n    order = matched.columns.tolist() + self._df.columns.tolist()\n    ret.sort_index(\n        key=lambda cols: [order.index(c) for c in cols],\n        axis=1,\n        inplace=True,\n    )\n\n    # Return dataframe.\n    return ret\n</code></pre>"},{"location":"examples/","title":"Examples","text":"<p>Here is a list of examples of techno-economic assessments made with TEAM. The examples can be downloaded and executed as Jupyter notebooks.</p> <ul> <li>green-lcoh</li> </ul>"},{"location":"examples/green-lcoh/","title":"Calculating LCOH","text":"<p>Here we calculate the Levelised Cost of Hydrogen (LCOH) for electrolytic hydrogen.</p> In\u00a0[1]: Copied! <pre>import numpy as np\nimport pandas as pd\nimport plotly\n\npd.options.plotting.backend = \"plotly\"\n\nfrom cet_units import Q\nfrom posted import TEDF\nfrom team.tools import calc_LCOX\n</pre> import numpy as np import pandas as pd import plotly  pd.options.plotting.backend = \"plotly\"  from cet_units import Q from posted import TEDF from team.tools import calc_LCOX In\u00a0[2]: Copied! <pre>tech_ely = TEDF.load(\"Tech|Electrolysis\").aggregate(\n    subtech=[\"AEL\", \"PEM\"],\n    size=\"100 MW\",\n    append_references=True,\n    agg=[\"source\", \"subtech\"],\n)\ndisplay(tech_ely)\n</pre> tech_ely = TEDF.load(\"Tech|Electrolysis\").aggregate(     subtech=[\"AEL\", \"PEM\"],     size=\"100 MW\",     append_references=True,     agg=[\"source\", \"subtech\"], ) display(tech_ely)          Loading ITables v2.8.1 from the internet...         (need help?)        In\u00a0[3]: Copied! <pre>assumptions = pd.concat([\n    pd.DataFrame().from_records([\n        {\"variable\": \"Price|Water\", \"value\": 10.0, \"unit\": \"EUR_2024/t_H2O\"},\n        {\"variable\": \"OCF\", \"value\": 50.0, \"unit\": \"percent\"},\n    ]),\n    pd.DataFrame({\"price_case\": [\"low\", \"high\"], \"variable\": \"Price|Electricity\", \"value\": [30.0, 60.0], \"unit\": \"EUR_2024/MWh\"}),\n], ignore_index=True).team.sort_columns()\ndisplay(assumptions)\n</pre> assumptions = pd.concat([     pd.DataFrame().from_records([         {\"variable\": \"Price|Water\", \"value\": 10.0, \"unit\": \"EUR_2024/t_H2O\"},         {\"variable\": \"OCF\", \"value\": 50.0, \"unit\": \"percent\"},     ]),     pd.DataFrame({\"price_case\": [\"low\", \"high\"], \"variable\": \"Price|Electricity\", \"value\": [30.0, 60.0], \"unit\": \"EUR_2024/MWh\"}), ], ignore_index=True).team.sort_columns() display(assumptions)          Loading ITables v2.8.1 from the internet...         (need help?)        In\u00a0[4]: Copied! <pre>df = (\n    tech_ely\n    .team.perform(calc_LCOX, reference=\"Output|Hydrogen\", using=assumptions, interest_rate=0.08, book_lifetime=\"20 years\", only_new=True)\n    .team.unit_to(\"EUR_2024/MWh_H2_LHV\")\n    .sort_values(by=\"price_case\", key=lambda col: col.apply([\"low\", \"high\"].index))\n)\ndisplay(df)\n\ndisplay(\n    df\n    .plot.bar(\n        x=\"price_case\",\n        y=\"value\",\n        color=\"variable\",\n    )\n    .update_layout(\n        xaxis_title=\"Electricity price case\",\n        yaxis_title=f\"LCOH  ( {df.unit.iloc[0]} )\",\n    )\n)\n</pre> df = (     tech_ely     .team.perform(calc_LCOX, reference=\"Output|Hydrogen\", using=assumptions, interest_rate=0.08, book_lifetime=\"20 years\", only_new=True)     .team.unit_to(\"EUR_2024/MWh_H2_LHV\")     .sort_values(by=\"price_case\", key=lambda col: col.apply([\"low\", \"high\"].index)) ) display(df)  display(     df     .plot.bar(         x=\"price_case\",         y=\"value\",         color=\"variable\",     )     .update_layout(         xaxis_title=\"Electricity price case\",         yaxis_title=f\"LCOH  ( {df.unit.iloc[0]} )\",     ) )          Loading ITables v2.8.1 from the internet...         (need help?)        In\u00a0[5]: Copied! <pre>assumptions = pd.concat([\n    pd.DataFrame().from_records([\n        {\"variable\": \"Price|Electricity\", \"value\": 50.0, \"unit\": \"EUR_2024/MWh\"},\n        {\"variable\": \"Price|Water\", \"value\": 10.0, \"unit\": \"EUR_2024/t_H2O\"},\n    ]),\n    pd.DataFrame({\"ocf\": pd.Series(range(10, 100)), \"variable\": \"OCF\", \"value\": np.arange(10.0, 100.0), \"unit\": \"percent\"}),\n])\n</pre> assumptions = pd.concat([     pd.DataFrame().from_records([         {\"variable\": \"Price|Electricity\", \"value\": 50.0, \"unit\": \"EUR_2024/MWh\"},         {\"variable\": \"Price|Water\", \"value\": 10.0, \"unit\": \"EUR_2024/t_H2O\"},     ]),     pd.DataFrame({\"ocf\": pd.Series(range(10, 100)), \"variable\": \"OCF\", \"value\": np.arange(10.0, 100.0), \"unit\": \"percent\"}), ]) In\u00a0[6]: Copied! <pre>df = (\n    tech_ely\n    .team.perform(calc_LCOX, reference=\"Output|Hydrogen\", using=assumptions, interest_rate=0.08, book_lifetime=\"20 years\", only_new=True)\n    .team.unit_to(\"EUR_2024/kg_H2\")\n    .team.sum_over(\"variable\")\n)\ndisplay(df)\n\ndisplay(\n    df\n    .plot.line(\n        x=\"ocf\",\n        y=\"value\",\n    )\n    .update_layout(\n        xaxis_title=\"Operational Capacity Factor  (%)\",\n        yaxis_title=f\"LCOH  ( {df.unit.iloc[0]} )\",\n    )\n)\n</pre> df = (     tech_ely     .team.perform(calc_LCOX, reference=\"Output|Hydrogen\", using=assumptions, interest_rate=0.08, book_lifetime=\"20 years\", only_new=True)     .team.unit_to(\"EUR_2024/kg_H2\")     .team.sum_over(\"variable\") ) display(df)  display(     df     .plot.line(         x=\"ocf\",         y=\"value\",     )     .update_layout(         xaxis_title=\"Operational Capacity Factor  (%)\",         yaxis_title=f\"LCOH  ( {df.unit.iloc[0]} )\",     ) )          Loading ITables v2.8.1 from the internet...         (need help?)"},{"location":"examples/green-lcoh/#calculating-lcoh","title":"Calculating LCOH\u00b6","text":""},{"location":"examples/green-lcoh/#prepare-notebook","title":"Prepare notebook.\u00b6","text":""},{"location":"examples/green-lcoh/#obtain-data","title":"Obtain data\u00b6","text":""},{"location":"examples/green-lcoh/#choose-assumptions","title":"Choose assumptions\u00b6","text":""},{"location":"examples/green-lcoh/#calculate-lcoh-for-different-electricity-prices","title":"Calculate LCOH for different electricity prices\u00b6","text":""},{"location":"examples/green-lcoh/#calculate-lcoh-for-different-capacity-factors","title":"Calculate LCOH for different capacity factors\u00b6","text":""},{"location":"guide/overview/","title":"User Guide","text":"<p>A comprehensive guide will be made available here at a later stage.</p>"}]}